DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
export PATH=$DIR/../:$PATH
export PYOPENCL_CTX=''

module load Anaconda3/5.0.0.1
module load gcc/4.8
module load cuda/8.0
pip install -r $DIR/environment.txt --user
module load paraview/5.2
sviz